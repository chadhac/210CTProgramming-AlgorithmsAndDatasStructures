#Week 6
#Question 12

#Tree sort algorithm
#Pre order
#In order
#Post order

class treeSort():
    """Function to implement tree sort algorithm"""

def preOrder():
    if self.left_child:
        self.left_child.preorder()
    if self.right_child:
        self.right_child.preorder()
    
def inOrder():
    if treeSort != None:
        order(treeSort.left_child())
        order(tree.right_child())
        print(tree.root_val())
    
def postOrder():
    if treeSort != None:
        order(treeSort.left_child())
        order(tree.right_child())
        print(tree.root_val())


treeSort()
